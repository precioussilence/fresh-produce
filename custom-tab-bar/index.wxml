<!--custom-tab-bar/index.wxml-->
<t-tab-bar
  t-class="t-tab-bar"
  value="{{selected}}"
  bindchange="onChange"
  theme="tag"
  split="{{false}}"
>
  <t-tab-bar-item wx:for="{{list}}" wx:key="index" value="{{index}}" icon="{{item.icon}}">
    {{item.text}}
  </t-tab-bar-item>
</t-tab-bar>
